require "spec_helper"

describe IPgeolocation do
  it "has a version number" do
    expect(IPgeolocation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
